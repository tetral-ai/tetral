# Tetral AI Engine

Agent guidance for this repository lives in [AGENTS.md](AGENTS.md) — the doc
index, service map, build/test commands, and contribution rules. Read it first.
Each service's contract is its own `services/<name>/README.md`.
